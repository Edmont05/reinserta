import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF3949AB);    // Azul (Primario)
  static const Color secondary = Color(0xFF2E7D32);  // Verde (Secundario)
  static const Color accent = Color(0xFFFFB300);     // Amber (Acento)
  static const Color background = Color(0xFFF9FAFB); // Fondo
  static const Color warning = Color(0xFFFF7043);    // Coral (Aviso)
  static const Color amber = Color(0xFFFFB300);      // Amber (Acento)
  static const Color coral = Color(0xFFFF7043);      // Coral (Aviso)
  static const Color cardBg = Colors.white;          // Fondo tarjetas
  static const Color cardBorder = Color(0xFFE0E0E0); // Borde tarjetas
  static const Color progressBg = Color(0xFFD6E4F0); // Fondo barra progreso
  static const Color reliableBg = Color(0xFFF3F6FA); // Fondo bloque confiable
  static const Color reliableIconBg = Color(0xFFE3F2FD); // Fondo icono confiable
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5B5B5B);
}